# RESULT: ORD-011

order_id: ORD-011
case_id: ACF-RELIABILITY-SUPERPOWERS
status: complete
plan_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/PLAN.md
order_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/orders/ORD-011.order.md
expected_result_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/results/ORD-011.result.md

## Summary

Implemented a machine-readable mitigation acceptance path for the Codex `exec` `UserPromptSubmit` runtime residual. The system still reports native `UserPromptSubmit` proof separately and does not claim it is fixed. Objective audit can now treat non-interactive Codex execution as covered only when three facts are all true: wrapper preflight blocks rejected prompts, the guarded launcher is distributed, and an explicit mitigation acceptance artifact exists.

Closed `INC-001-codex-exec-user-prompt-submit-not-observed.md` under that accepted guarded-launcher policy. Closure means the incident no longer blocks the objective; it does not erase the native runtime limitation.

## Changes

- Added objective-audit checks for `codex-exec-mitigation-acceptance` and `codex-user-prompt-submit-coverage`.
- Kept `codex-user-prompt-submit-proof` as a raw native runtime status check; it can remain fail while coverage passes via accepted mitigation.
- Updated objective-audit failure handling so the raw native proof failure is non-blocking only when coverage passes.
- Added tests for unresolved native failure without mitigation and accepted mitigation with native failure.
- Added `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-011-codex-exec-mitigation.json`.
- Closed `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/incidents/INC-001-codex-exec-user-prompt-submit-not-observed.md` with explicit closure rationale.

## Evidence

- `PYTHONPATH=src pytest -q tests/test_acceptance.py -k 'user_prompt_submit_probe_is_runtime_fail or mitigation_for_runtime_fail'`: `2 passed, 16 deselected`.
- `PYTHONPATH=src pytest -q tests/test_acceptance.py -k objective_audit`: `7 passed, 11 deselected`.
- `PYTHONPATH=src pytest -q`: `124 passed in 0.63s`.
- `PYTHONPATH=src python3 -m agent_careflow.cli acceptance objective-audit --case ACF-RELIABILITY-SUPERPOWERS`: Codex residual checks passed after mitigation acceptance; dashboard/workspace awaited this result file before regeneration; Claude review checks still failed.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-011-codex-exec-mitigation.json`: accepted guarded launcher policy.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-011-objective-audit.txt`: final objective audit evidence; Codex residual checks pass under accepted mitigation, Claude review checks still fail.

## Residual Limitations

- Native Codex `UserPromptSubmit` remains runtime-observed failing for the probed `codex exec` surface. This is now a tracked native limitation with accepted non-interactive wrapper coverage, not a claimed native fix.
- A real non-placeholder Claude review remains missing, so the overarching objective audit is still expected to fail.

## Completion Note

ORD-011 is complete because Codex `exec` runtime residual handling is now explicit, evidence-backed, test-covered, and reflected in incident status. The overall ACF-RELIABILITY-SUPERPOWERS objective remains incomplete until the Claude review gate is satisfied or the business/private profile policy is split so Codex-only environments are audited separately from business dual-review environments.
