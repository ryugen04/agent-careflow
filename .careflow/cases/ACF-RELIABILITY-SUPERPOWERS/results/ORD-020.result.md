# RESULT: ORD-020

order_id: ORD-020
case_id: ACF-RELIABILITY-SUPERPOWERS
status: complete
plan_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/PLAN.md
order_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/orders/ORD-020.order.md
expected_result_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/results/ORD-020.result.md

## Summary

Fixed the `review claude-run` runtime path so the command no longer stops at the known Claude CLI default drift where `claude-opus-4-6` with `thinking.type=enabled` exits before authentication. When no explicit model is supplied and that exact deprecated configuration error appears, `run_claude_review()` now retries once with fallback model `sonnet`.

Explicit `--model` remains authoritative and is not silently changed. Authentication failures remain hard failures and do not create placeholder reviews.

## Changes

- Added `DEFAULT_CLAUDE_REVIEW_FALLBACK_MODEL = "sonnet"` and narrow deprecated-error detection in `src/agent_careflow/claude_review.py`.
- Refactored Claude command construction into helper functions so retry behavior is explicit and testable.
- Added regression tests proving default deprecated thinking errors retry with `--model sonnet` and explicit model failures do not retry.

## Evidence

- `PYTHONPATH=src pytest -q tests/test_claude_review.py`: `5 passed in 0.08s`.
- `PYTHONPATH=src pytest -q`: `143 passed in 0.74s`.
- `PYTHONPATH=src python3 -m py_compile src/agent_careflow/claude_review.py`: passed.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-020-claude-review-default-after-fallback.txt`: default `review claude-run` now reaches `401 Invalid authentication credentials` instead of the deprecated `thinking.type=enabled` error.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-020-claude-review-default-after-fallback.status`: exit status `1`, as expected while Claude auth is invalid.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-020-review-status-business.txt`: business review status still requires real strict Claude review.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-020-objective-matrix-private.txt`: final private objective matrix evidence after dashboard/workspace refresh.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-020-objective-matrix-business.txt`: final business objective matrix evidence after dashboard/workspace refresh.

## Residual Limitations

- This order does not fix Claude authentication. The true remaining business blocker is still `401 Invalid authentication credentials` for the local Claude CLI.
- There is still no strict non-placeholder `REVIEW-CLAUDE-ORD-018.review.md` artifact.
- Business objective audit and matrix must continue to fail until a Claude-authenticated environment produces or imports that review artifact.

## Completion Note

ORD-020 is complete because `review claude-run` now bypasses the known deprecated default model/thinking failure and reaches the real auth boundary. The overarching business-mode goal remains incomplete because strict Claude review evidence is still missing.
