# RESULT: ORD-018

order_id: ORD-018
case_id: ACF-RELIABILITY-SUPERPOWERS
status: complete
plan_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/PLAN.md
order_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/orders/ORD-018.order.md
expected_result_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/results/ORD-018.result.md

## Summary

Added `agent-careflow acceptance objective-matrix` so the broad original objective is no longer judged only by a compact pass/fail audit. The matrix maps each captured user requirement to concrete acceptance checks and evidence paths, including unified Claude/Codex settings, durable plans, `.agent` workspace outputs, learnings/incidents, fixed handoffs, ORDER-as-subplan behavior, Superpowers-style enforcement, activation/forgetting mitigation, multi-repo/worktree support, guarded launchers, review policy, and the completion audit itself.

The command supports text and JSON output. It preserves profile semantics: private/Codex-only can pass with Codex review and accepted Codex exec mitigation, while business remains strict until a real non-placeholder Claude review exists.

## Changes

- Added objective requirement definitions and `run_objective_matrix()` in `src/agent_careflow/acceptance.py`.
- Added `agent-careflow acceptance objective-matrix --profile business|private --format text|json` in `src/agent_careflow/cli.py`.
- Added `claude-careflow-launcher-distribution` to objective-audit so ORD-017 Claude launcher evidence is part of the end-to-end audit.
- Added tests proving objective matrix coverage, private profile pass behavior, business strict Claude review failure, and Claude launcher audit coverage.
- Updated README acceptance commands and objective-audit documentation to include the matrix.

## Evidence

- `PYTHONPATH=src pytest -q tests/test_acceptance.py -k 'objective_matrix or objective_audit_passes or install_audit'`: `8 passed, 15 deselected in 0.12s`.
- `PYTHONPATH=src pytest -q`: `141 passed in 0.73s`.
- `PYTHONPATH=src python3 -m py_compile src/agent_careflow/acceptance.py src/agent_careflow/cli.py`: passed.
- `PYTHONPATH=src python3 -m agent_careflow.cli acceptance --help`: lists `objective-matrix`.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-018-objective-matrix-private.txt`: final private objective matrix text evidence; `acceptance=pass`.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-018-objective-matrix-private.json`: final private objective matrix machine-readable evidence.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-018-objective-matrix-business.txt`: final business objective matrix text evidence; strict failure remains on real Claude review.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-018-objective-matrix-business.json`: final business objective matrix machine-readable evidence.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-018-objective-audit-private.txt`: final private objective audit evidence; `acceptance=pass`.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-018-objective-audit-business.txt`: final business objective audit evidence; strict failure remains on placeholder/missing real Claude review.
- JSON validation for both objective matrix files with `python3 -m json.tool`: passed.

## Residual Limitations

- Business profile still fails until a real non-placeholder Claude review is generated or imported from a Claude-authenticated environment.
- The accepted Codex `exec` UserPromptSubmit mitigation remains explicit: native proof is still recorded as failing for that mode, while guarded launcher coverage is accepted.

## Completion Note

ORD-018 is complete because every captured high-level requirement is now represented by a durable matrix row with concrete audit checks and evidence paths. This advances the full objective, but the overarching business-mode goal remains incomplete until strict Claude review evidence exists.
