# RESULT: ORD-021

order_id: ORD-021
case_id: ACF-RELIABILITY-SUPERPOWERS
status: complete
plan_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/PLAN.md
order_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/orders/ORD-021.order.md
expected_result_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/results/ORD-021.result.md

## Summary

Added `agent-careflow review claude-auth` as an explicit Claude authentication preflight. The command uses the same safe fallback behavior as `review claude-run`: if the default Claude CLI path hits the known deprecated `claude-opus-4-6` plus `thinking.type=enabled` error, it retries with `sonnet` unless the user explicitly supplied a model.

`review status --profile business` now suggests the auth preflight before export/run/import commands, so a later session can distinguish local Claude authentication failure from careflow review workflow failure.

## Changes

- Added `check_claude_auth()` and `render_claude_auth_report()` in `src/agent_careflow/claude_review.py`.
- Added `agent-careflow review claude-auth --model sonnet --format text|json` in `src/agent_careflow/cli.py`.
- Added `agent-careflow review claude-auth --model sonnet` to business review status next commands.
- Added tests for auth preflight pass/fail reporting, fallback behavior, and review status guidance.
- Updated README review workflow documentation with the preflight command.

## Evidence

- `PYTHONPATH=src pytest -q tests/test_claude_review.py tests/test_review_status.py`: `10 passed in 0.10s`.
- `PYTHONPATH=src pytest -q`: `145 passed in 0.75s`.
- `PYTHONPATH=src python3 -m py_compile src/agent_careflow/claude_review.py src/agent_careflow/cli.py src/agent_careflow/review_status.py`: passed.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-021-claude-auth.txt`: local text preflight evidence; `claude-auth=fail` with `401 Invalid authentication credentials`.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-021-claude-auth.json`: machine-readable preflight evidence with schema `agent-careflow.claude_auth.v1`, `status: fail`, and exit code `1`.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-021-review-status-business.txt`: business review status now includes `agent-careflow review claude-auth --model sonnet` as the first next command.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-021-objective-matrix-private.txt`: final private objective matrix evidence.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-021-objective-matrix-business.txt`: final business objective matrix evidence.

## Residual Limitations

- This order does not fix external Claude authentication. The local CLI still returns `401 Invalid authentication credentials`.
- There is still no strict non-placeholder `REVIEW-CLAUDE-ORD-018.review.md` artifact.
- Business objective audit and matrix must continue to fail until a Claude-authenticated environment produces or imports that review artifact.

## Completion Note

ORD-021 is complete because Claude auth state is now explicitly diagnosable before review execution, and the business review status points operators to that preflight. The overarching business-mode goal remains incomplete because strict Claude review evidence is still missing.
