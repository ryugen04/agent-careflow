# RESULT: ORD-009

order_id: ORD-009
case_id: ACF-RELIABILITY-SUPERPOWERS
status: complete
plan_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/PLAN.md
order_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/orders/ORD-009.order.md
expected_result_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/results/ORD-009.result.md

## Summary

Implemented a guarded Codex non-interactive entrypoint: `agent-careflow codex exec`. It runs the same prompt policy used by Codex `UserPromptSubmit` hooks before invoking `codex exec`, records JSONL runtime evidence, and blocks rejected prompts before Codex is called.

This is a mitigation for the observed `codex exec` UserPromptSubmit gap, not proof that the native Codex hook fires.

## Changes

- Added `src/agent_careflow/codex_wrapper.py` with prompt extraction, prompt policy preflight, dry-run support, Codex subprocess invocation, and `codex.runtime_probe.v1` evidence writing.
- Added `agent-careflow codex exec` CLI command with `--dry-run`, `--codex-bin`, and `--probe-output`.
- Added objective audit check `codex-wrapper-preflight`, separate from native `codex-user-prompt-submit-proof`.
- Updated README with guarded Codex entrypoint usage and its limitations.
- Added focused tests for prompt extraction, blocking rejected prompts, allowing benign prompts, probe JSONL, and objective audit wrapper evidence.
- Recorded active wrapper evidence at `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-009-codex-wrapper-preflight.jsonl`.

## Evidence

- `PYTHONPATH=src python -m agent_careflow.cli codex exec --dry-run --probe-output .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-009-codex-wrapper-preflight.jsonl -- --json 'debug patient MRN: ABC123456 in this fixture'`: exit 2; blocked before Codex invocation.
- `python3 /home/glaucus03/dev/projects/dotfiles/packages/agents/.agents/skills/codex-runtime-probing/scripts/validate_probe_log.py .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-009-codex-wrapper-preflight.jsonl`: `probe log valid`.
- `PYTHONPATH=src python -m agent_careflow.cli codex exec --dry-run --probe-output /tmp/agent-careflow-wrapper-allow.jsonl -- --json 'refactor validator tests'`: exit 0; `dry-run: prompt allowed`.
- `python3 -m py_compile src/agent_careflow/codex_wrapper.py src/agent_careflow/cli.py src/agent_careflow/acceptance.py tests/test_codex_wrapper.py tests/test_acceptance.py`: passed.
- `PYTHONPATH=src pytest -q tests/test_codex_wrapper.py tests/test_acceptance.py -k 'codex_wrapper or objective_audit or wrapper_preflight'`: `9 passed, 10 deselected`.
- `PYTHONPATH=src pytest -q`: `121 passed in 0.62s`.
- `PYTHONPATH=src python -m agent_careflow.cli acceptance objective-audit --case ACF-RELIABILITY-SUPERPOWERS`: `codex-wrapper-preflight` passed; native `codex-user-prompt-submit-proof` still failed.

## Residual Limitations

- Native Codex `UserPromptSubmit` remains runtime-observed failing for the probed `codex exec` surface.
- The open incident `INC-001-codex-exec-user-prompt-submit-not-observed.md` remains open because the wrapper is a mitigation, not a native hook fix.
- A real non-placeholder Claude review remains missing.

## Completion Note

ORD-009 is complete because the guarded entrypoint, tests, docs, evidence, and audit check exist and verify. The overarching reliability objective remains incomplete until native hook residuals are resolved or explicitly accepted as mitigated by installed wrapper policy, and until a real Claude review is produced.
