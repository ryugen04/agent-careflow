# RESULT: ORD-012

order_id: ORD-012
case_id: ACF-RELIABILITY-SUPERPOWERS
status: complete
plan_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/PLAN.md
order_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/orders/ORD-012.order.md
expected_result_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/results/ORD-012.result.md

## Summary

Implemented profile-aware objective audit behavior for the user's business/private operating modes. The default `business` profile remains strict and requires non-placeholder Codex and Claude reviews. The `private` profile validates Codex-only review evidence and does not fail solely because Claude is unavailable.

This makes the audit model match the actual requirement instead of forcing every machine into the business review policy.

## Changes

- Added `profile` support to `run_objective_audit`, with accepted values `business` and `private`.
- Added `agent-careflow acceptance objective-audit --profile business|private`; default remains `business`.
- Made review checks profile-aware:
  - `business`: requires strict Codex+Claude review and rejects placeholder Claude review.
  - `private`: requires strict Codex review only and skips Claude review gates.
- Included profile in objective-audit text output.
- Added tests proving business remains strict and private allows Codex-only review.

## Evidence

- `PYTHONPATH=src pytest -q tests/test_acceptance.py -k 'business_profile_requires or private_profile_allows'`: `2 passed, 18 deselected`.
- `PYTHONPATH=src pytest -q tests/test_acceptance.py -k objective_audit`: `9 passed, 11 deselected`.
- `PYTHONPATH=src pytest -q`: `126 passed in 0.66s`.
- `PYTHONPATH=src python3 -m agent_careflow.cli acceptance objective-audit --case ACF-RELIABILITY-SUPERPOWERS --profile private`: after dashboard regeneration, expected to pass for Codex-only profile.
- `PYTHONPATH=src python3 -m agent_careflow.cli acceptance objective-audit --case ACF-RELIABILITY-SUPERPOWERS --profile business`: expected to continue failing on placeholder Claude review until a real Claude review exists.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-012-objective-audit-private.txt`: private/Codex-only objective audit passed.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-012-objective-audit-business.txt`: business objective audit failed only on placeholder/missing real Claude review gates.

## Residual Limitations

- Business profile still lacks a real non-placeholder Claude review on this machine.
- Native Codex `UserPromptSubmit` remains a native runtime limitation, covered for non-interactive use by accepted guarded-launcher mitigation.

## Completion Note

ORD-012 is complete when private and business objective-audit evidence is recorded after dashboard/workspace regeneration. The overarching business-mode goal remains incomplete until a real Claude review artifact replaces the placeholder review.
