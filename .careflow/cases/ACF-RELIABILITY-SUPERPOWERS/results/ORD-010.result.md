# RESULT: ORD-010

order_id: ORD-010
case_id: ACF-RELIABILITY-SUPERPOWERS
status: complete
plan_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/PLAN.md
order_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/orders/ORD-010.order.md
expected_result_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/results/ORD-010.result.md

## Summary

Implemented distribution for the guarded Codex entrypoint through dotfiles. `install.sh codex` now installs a managed `~/.local/bin/codex-careflow` launcher that delegates to `agent-careflow codex exec` without replacing or shadowing the real `codex` binary.

The launcher is treated as install-audit evidence and objective-audit evidence, so future sessions can detect whether the mitigation is actually distributed instead of relying on a remembered instruction.

## Changes

- Updated `/home/glaucus03/dev/projects/dotfiles/install.sh` to install/remove a managed `codex-careflow` launcher alongside Codex hooks, config, and careflow skill links.
- Updated dotfiles README and `codex-plan.md` to describe the guarded launcher and the install-audit gate.
- Added `codex-careflow-launcher` to `agent-careflow acceptance install-audit`.
- Added `codex-careflow-launcher-distribution` to `agent-careflow acceptance objective-audit`, backed by install-audit JSON evidence.
- Added tests proving install audit passes when the launcher exists and fails when it is missing.

## Evidence

- `bash -n install.sh`: passed.
- `PYTHONPATH=src pytest -q tests/test_acceptance.py -k install_audit`: `4 passed, 12 deselected`.
- `PYTHONPATH=src pytest -q tests/test_acceptance.py -k 'install_audit or objective_audit'`: `10 passed, 7 deselected`.
- `PYTHONPATH=src pytest -q`: `123 passed in 0.63s`.
- `AGENT_CAREFLOW_REPO=/home/glaucus03/dev/projects/agent-careflow DOTFILES_TARGET_HOME=/tmp/agent-careflow-ord-010-home.zqI6hh ./install.sh -n codex`: dry-run included `install guarded Codex launcher`.
- `AGENT_CAREFLOW_REPO=/home/glaucus03/dev/projects/agent-careflow DOTFILES_TARGET_HOME=/tmp/agent-careflow-ord-010-home.zqI6hh ./install.sh codex`: installed `/tmp/agent-careflow-ord-010-home.zqI6hh/.local/bin/codex-careflow`.
- `PYTHONPATH=src python3 -m agent_careflow.cli acceptance install-audit --target-home /tmp/agent-careflow-ord-010-home.zqI6hh --tool codex`: passed, including `codex-careflow-launcher`.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-010-install-audit.json`: machine-readable install audit evidence.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-010-launcher-install-evidence.md`: launcher file, safe dry-run, and rejected prompt dry-run evidence.
- `.careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-010-objective-audit.txt`: objective audit evidence; `codex-careflow-launcher-distribution` passed, while known native `UserPromptSubmit`, Claude review, and open incident gates failed.

## Residual Limitations

- Native Codex `UserPromptSubmit` remains runtime-observed failing for the probed `codex exec` surface; this launcher is a distributed mitigation, not a native hook fix.
- The open incident `INC-001-codex-exec-user-prompt-submit-not-observed.md` remains open.
- A real non-placeholder Claude review remains missing, so the overarching objective audit is still expected to fail.

## Completion Note

ORD-010 is complete because the guarded launcher is distributed through dotfiles, verified against a fixture home, audited by install/objective audit checks, documented, and covered by tests. The overall ACF-RELIABILITY-SUPERPOWERS objective remains incomplete until the native hook residual and Claude review gate are resolved or explicitly accepted by the user as residual risk.
