# ORDER: ORD-024

order_id: ORD-024
case_id: ACF-RELIABILITY-SUPERPOWERS
assigned_role: implementer
plan_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/PLAN.md
plan_hash: sha256:30fc0b0ae8102b1a548e8fb76f2148dad4e126d169eede1987b41b7f2430d7b1
allowed_actions:
  - inspect PR #1 metadata, branch state, and local worktree diffs
  - verify the current private and business review/acceptance gates
  - run Claude auth diagnostics without attempting subscription or login remediation
  - harden doctor checks for installed Codex hook/config locations and ai-dlc workspaces
  - harden hook missing-context handling for non-policy tools without weakening mutating-tool enforcement
  - record verification evidence and the remaining external Claude review requirement
  - update generated dashboard/workspace artifacts
  - commit and push the PR branch after verification
forbidden_actions:
  - edit outside the agent-careflow PR branch worktree for this order
  - overwrite or reset the main worktree's pre-existing uncommitted changes
  - weaken strict business Claude review requirements
  - fabricate or synthesize Claude review evidence from a non-Claude reviewer
  - mark business acceptance complete while strict Claude review evidence is missing
  - modify the separate dotfiles repository
deliverables:
  - .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/results/ORD-024.result.md
  - PR branch commit with focused fixes and updated evidence
expected_result_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/results/ORD-024.result.md
completion_criteria:
  - private review status and private objective matrix pass
  - business review status/objective matrix are either satisfied by a real strict Claude review or fail with clear external-review evidence
  - Claude auth diagnostic is recorded
  - doctor and hook focused tests pass
  - full test suite passes
  - result artifact records exact commands, statuses, and residual limitation
  - branch is ready for PR #1 continuation

## Handoff Header

PLAN_FILE: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/PLAN.md
ORDER_FILE: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/orders/ORD-024.order.md
SUBPLAN_FILE: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/orders/ORD-024.order.md
EXPECTED_RESULT_PATH: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/results/ORD-024.result.md
CASE_ID: ACF-RELIABILITY-SUPERPOWERS
ORDER_ID: ORD-024
ASSIGNED_ROLE: implementer
TARGET_TOOL: codex

## Summary

Continue PR #1 after publication. The PR body records residual work: business-mode completion is blocked until a real strict Claude review artifact is imported. This order verifies whether the current environment can produce that review, keeps the strict gate intact, and applies only non-fabricating hardening fixes that move the PR closer to usable completion.

## Fixed Input / Concrete Scenario

- Repository: `ryugen04/agent-careflow`
- PR: #1, branch `codex/agent-careflow-reliability`
- Case: `ACF-RELIABILITY-SUPERPOWERS`
- Previous order: `ORD-023`
- Continuation order: `ORD-024`
- Review gate: private requires Codex; business requires Codex and Claude strict review artifacts.
- Current environment command: `agent-careflow review claude-auth --model sonnet`

## Background

- `ORD-023` opened the draft PR and intentionally documented the missing strict Claude review artifact.
- The PLAN decision says Claude auth/CLI remediation remains out of scope unless explicitly reopened.
- The user asked to continue the work to completion according to the PR.
- A separate main worktree contains uncommitted edits; this order must preserve them and work in the PR branch worktree.

## Expected Output by Surface

- Review surface: private profile stays satisfied; business profile must only become satisfied after a real strict Claude review import.
- Hook surface: non-mutating tools with no command or file path, such as `functions.update_plan`, are allowed even in fail-closed mode; mutating tools with no extractable path remain blocked in hybrid/deny mode.
- Doctor surface: normal agent-careflow repos validate as before; ai-dlc workspaces validate installed Codex config/hooks and declared workspace paths without requiring agent-careflow source schemas.
- Evidence surface: ORD-024 records command output files for auth, review status, objective matrices, doctor, focused tests, full tests, result validation, order status, dashboard, and workspace generation.
- PR surface: PR #1 keeps the residual work honest and includes the pushed ORD-024 fixes.

## Forbidden Outputs / Regression Guards

- Do not replace placeholder Claude review text with non-Claude output.
- Do not loosen `validate_review(..., strict=True)` or acceptance matrix business review requirements.
- Do not allow pathless mutating tools such as `apply_patch`, `Edit`, `Write`, or `MultiEdit`.
- Do not remove SessionStart, Stop, hybrid hook behavior, careflow context resolution, or fixed handoff labels.
- Do not include dotfiles repository changes.

## Implementation Flow

1. Record current PR and gate diagnostics.
2. Add focused tests for pathless non-policy tools and ai-dlc doctor acceptance.
3. Update hook and doctor implementations conservatively.
4. Run focused tests, full tests, and acceptance gates.
5. Write ORD-024 result and regenerate dashboard/workspace.
6. Commit and push the PR branch if verification supports it.

## Review Gates

- Tests must catch both the allowed non-policy tool path and the still-blocked mutating path.
- Doctor tests must cover both normal repo hooks and ai-dlc workspace installed hooks/config.
- Business gate evidence must remain failing unless a real strict Claude review artifact is imported.
- Existing PR branch functionality must not be regressed by copying stale main-worktree files wholesale.

## Verification

- `PYTHONPATH=src pytest -q tests/test_hooks.py tests/test_doctor.py`
- `PYTHONPATH=src pytest -q`
- `PYTHONPATH=src python3 -m agent_careflow.cli doctor`
- `PYTHONPATH=src python3 -m agent_careflow.cli review status --case ACF-RELIABILITY-SUPERPOWERS --profile private --order ORD-024`
- `PYTHONPATH=src python3 -m agent_careflow.cli review status --case ACF-RELIABILITY-SUPERPOWERS --profile business --order ORD-024`
- `PYTHONPATH=src python3 -m agent_careflow.cli review claude-auth --model sonnet`
- `PYTHONPATH=src python3 -m agent_careflow.cli acceptance objective-matrix --case ACF-RELIABILITY-SUPERPOWERS --profile private`
- `PYTHONPATH=src python3 -m agent_careflow.cli acceptance objective-matrix --case ACF-RELIABILITY-SUPERPOWERS --profile business`

## Assumptions

- The user's continuation request reopens implementation/settings hardening work, but does not authorize fabricating review evidence.
- Claude login remains an external user/environment action unless the user explicitly provides an authenticated Claude environment.
- A branch can be pushed through the existing GitHub remote once local verification is complete.
