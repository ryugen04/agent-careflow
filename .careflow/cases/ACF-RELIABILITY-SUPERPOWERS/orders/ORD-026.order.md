# ORDER: ORD-026

order_id: ORD-026
case_id: ACF-RELIABILITY-SUPERPOWERS
assigned_role: reviewer
plan_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/PLAN.md
plan_hash: sha256:30fc0b0ae8102b1a548e8fb76f2148dad4e126d169eede1987b41b7f2430d7b1
allowed_actions:
  - verify Claude CLI authentication in the current environment
  - run a real Claude strict review for ORD-024 using the authenticated Claude CLI
  - validate the resulting Claude review artifact in strict mode
  - require Codex and Claude reviews in strict business mode
  - re-run private and business objective matrices
  - refresh dashboard/workspace artifacts
  - update PR #1 body after business gate completion
  - commit and push the PR branch after verification
  - write expected result
forbidden_actions:
  - edit outside the agent-careflow PR branch worktree for this order
  - overwrite or reset the main worktree's pre-existing uncommitted changes
  - weaken strict business Claude review requirements
  - fabricate or synthesize Claude review evidence from a non-Claude reviewer
  - mark business acceptance complete without strict review validation evidence
  - modify the separate dotfiles repository
deliverables:
  - .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/results/ORD-026.result.md
  - .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/reviews/REVIEW-CLAUDE-ORD-024.review.md
  - .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/evidence/ORD-026-*
expected_result_path: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/results/ORD-026.result.md
completion_criteria:
  - Claude auth succeeds in the execution context used for review
  - real Claude review artifact exists and passes strict validation
  - business review status is satisfied for Codex and Claude
  - private and business objective matrices pass
  - result artifact records exact commands, statuses, and evidence paths
  - PR #1 is updated, pushed, and reflects no residual business-review blocker

## Handoff Header

PLAN_FILE: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/PLAN.md
ORDER_FILE: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/orders/ORD-026.order.md
SUBPLAN_FILE: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/orders/ORD-026.order.md
EXPECTED_RESULT_PATH: .careflow/cases/ACF-RELIABILITY-SUPERPOWERS/results/ORD-026.result.md
CASE_ID: ACF-RELIABILITY-SUPERPOWERS
ORDER_ID: ORD-026
ASSIGNED_ROLE: reviewer
TARGET_TOOL: codex

## Situation

The user corrected the prior blocker: this environment has Claude CLI logged in. Sandbox-local auth checks fail, but unsandboxed `claude auth status` and the exact `claude -p` probe succeed. Complete the PR residual work by generating a real strict Claude review artifact.

## Background

- `ORD-024` implemented the final hardening pass and exported an ORD-024 Claude review request.
- `ORD-025` recorded a completion audit but incorrectly treated local Claude auth as unavailable because the sandboxed probe could not access Claude login state.
- Business-mode completion requires a real non-placeholder strict Claude review artifact.
- The existing placeholder `REVIEW-CLAUDE-ORD-005.review.md` must not satisfy strict mode.

## Assessment

Run the Claude review through the authenticated CLI path, validate the artifact, then re-run all acceptance gates. If all gates pass, update the PR and mark the goal complete.

## Recommendation

Use `agent-careflow review claude-run --case ACF-RELIABILITY-SUPERPOWERS --order ORD-024 --review-id REVIEW-CLAUDE-ORD-024 --model sonnet` with escalation because Claude login/keychain access is outside the sandbox. Preserve all evidence.
